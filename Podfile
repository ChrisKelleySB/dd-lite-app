# Uncomment the next line to define a global platform for your project
# platform :ios, '12.1'

def app_pods
    pod 'SDWebImage', :modular_headers => true 
end

target 'dd-ios-takehome' do
  use_frameworks!
  app_pods

end
